
   
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Docker: Attach to Node",
            "type": "node",
            "request": "attach",
            "port": 9229,
            "cwd": "${workspaceFolder}/backend",
            "localRoot": "${workspaceFolder}/backend",
            "remoteRoot": "/app",
        }
    ]
}